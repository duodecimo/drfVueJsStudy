const state = { alo: "Alo!" };

const mutations = {};

const getters = {
  alo(state) {
    return state.alo;
  }
};

const actions = {};

// exportar
export default {
  state,
  mutations,
  getters,
  actions
};
